{{# section "temp_widget"}}
<div class="card mx-auto text-dark text-center " style="width: 100%">
    <img src="{{ data.current.condition.icon }}" class="rounded" alt="{{ data.current.condition.text }}">
    <div class="w-25 mx-auto">
        <h3 class="badge fs-5 bg-success">{{ data.current.temp_c }} &#8451;</h3>
    </div>
    <p class="card-text">{{ data.current.condition.text }}</p>
    <div class="card-body ">
        <h5 class="card-title display-6">{{ data.location.name }}</h5>
        <h5 class="card-title fs-6">{{ data.location.country }}</h5>
        <p class="badge fs-6 text-dark"> <b>{{ date.day }} / {{ date.month }} / {{ date.year }}</b></p>
        <p>Local time:</p>
        <div class="alert alert-success" role="alert">
            <b>{{ date.hour }} : {{ date.minute }}</b>
        </div>
        <p class="text-muted" >
            Powered by <a href="https://www.weatherapi.com/" title="Free Weather API" class="font-monospace fs-6">WeatherAPI.com</a>
        </p>
    </div>
</div>
{{/section}}

<div class="temp_table text-center table-responsive">
    <table class="table mt-4  table-hover">
        <thead>
        <tr class="table-success">
            <th scope="col">Time</th>
            <th scope="col">Temp </th>
            <th scope="col">Rain?</th>
            <th scope="col">Snow?</th>
            <th scope="col"></th>
        </tr>
        </thead>
        <tbody>

    {{#each temp}}
        <tr>
            <td >{{time}}</td>
            <th scope="row">{{temp_c}} &#8451;</th>
            <td>
                {{#if will_it_rain}}
                    <span class="bg-info h-100">Most likely it will rain</span>
                {{else}}
                    Not now
                {{/if}}
            </td>
            <td>
                {{#if will_it_snow}}
                    <span class="bg-info">Most likely it will snow </span>
                {{else}}
                    Not now
                {{/if}}
            </td>
            <td>
                <img src="{{condition.icon}} " alt="img">
            </td>
        </tr>
    {{/each}}
        </tbody>
    </table>
</div>


<!--button to UP-->
<button onclick="topFunction()" id="myBtn" title="Go to top">Top</button>